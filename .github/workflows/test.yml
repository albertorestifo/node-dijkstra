steps:
  - uses: actions/checkout@v3

  - uses: actions/setup-node@v3
    with:
      node-version: 16
      cache: "npm"

  - run: npm ci

  - run: npm test

  - uses: codecov/codecov-action@v3
    with:
      directory: ./coverage
